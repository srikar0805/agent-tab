# Agent Tab Chat

Multi-provider VS Code chat extension for agent-tab with live context and budget meters.

## Development

1. `npm install`
2. `npm run build`
3. Press `F5` in VS Code from the `extension/` folder to run the Extension Development Host.
